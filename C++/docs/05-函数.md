# 函数

函数是 C++ 程序的基本构建块，用于封装可重用的代码。C++ 提供了丰富的函数特性，包括函数重载、默认参数、lambda 表达式等。

## 函数定义

### 基本语法

```cpp
#include <iostream>

// 函数定义
// 返回类型 函数名(参数列表) { 函数体 }
int add(int a, int b) {
    return a + b;
}

// 无返回值函数
void print_message(const std::string& msg) {
    std::cout << msg << std::endl;
}

// 无参数函数
int get_answer() {
    return 42;
}

int main() {
    int result = add(3, 4);
    std::cout << "3 + 4 = " << result << std::endl;
    
    print_message("Hello, C++!");
    
    std::cout << "Answer: " << get_answer() << std::endl;
    
    return 0;
}
```

### 函数声明和定义

```cpp
#include <iostream>

// 函数声明（前向声明）
int multiply(int a, int b);

// 函数可以稍后定义
int main() {
    std::cout << multiply(5, 6) << std::endl;
    return 0;
}

// 函数定义
int multiply(int a, int b) {
    return a * b;
}
```

### 函数原型

```cpp
// 头文件 math.h
#ifndef MATH_H
#define MATH_H

// 函数原型（声明）
int add(int a, int b);
double calculate_area(double radius);
void print_result(double result);

#endif

// 实现文件 math.cpp
#include "math.h"
#include <iostream>
#include <cmath>

int add(int a, int b) {
    return a + b;
}

double calculate_area(double radius) {
    return 3.14159 * radius * radius;
}

void print_result(double result) {
    std::cout << "Result: " << result << std::endl;
}
```

## 参数传递

### 值传递（Pass by Value）

```cpp
#include <iostream>

// 值传递：传递参数的副本
void increment(int x) {
    x++;  // 只修改副本，不影响原变量
    std::cout << "Inside function: " << x << std::endl;
}

int main() {
    int num = 10;
    increment(num);
    std::cout << "Outside function: " << num << std::endl;
    // 输出：
    // Inside function: 11
    // Outside function: 10
    
    return 0;
}
```

### 引用传递（Pass by Reference）

```cpp
#include <iostream>

// 引用传递：传递变量的别名
void increment_ref(int& x) {
    x++;  // 修改原变量
    std::cout << "Inside function: " << x << std::endl;
}

// 常量引用：只读，避免复制（推荐用于大型对象）
void print_value(const int& x) {
    std::cout << "Value: " << x << std::endl;
    // x = 10;  // 错误！不能修改常量引用
}

int main() {
    int num = 10;
    increment_ref(num);
    std::cout << "Outside function: " << num << std::endl;
    // 输出：
    // Inside function: 11
    // Outside function: 11
    
    print_value(num);
    
    return 0;
}
```

### 指针传递（Pass by Pointer）

```cpp
#include <iostream>

// 指针传递：传递变量的地址
void increment_ptr(int* x) {
    if (x != nullptr) {  // 检查空指针
        (*x)++;  // 解引用并修改
        std::cout << "Inside function: " << *x << std::endl;
    }
}

int main() {
    int num = 10;
    increment_ptr(&num);  // 传递地址
    std::cout << "Outside function: " << num << std::endl;
    // 输出：
    // Inside function: 11
    // Outside function: 11
    
    return 0;
}
```

### 参数传递方式对比

| 方式 | 语法 | 优点 | 缺点 | 适用场景 |
|------|------|------|------|---------|
| **值传递** | `void f(int x)` | 安全，不修改原值 | 复制开销 | 小型对象 |
| **引用传递** | `void f(int& x)` | 高效，可修改 | 可能意外修改 | 需要修改参数 |
| **常量引用** | `void f(const int& x)` | 高效，只读 | 无 | 大型对象（推荐） |
| **指针传递** | `void f(int* x)` | 可传递 nullptr | 需要检查空指针 | C 风格接口 |

### 数组参数

```cpp
#include <iostream>

// 方式 1：传递数组指针和大小
void print_array1(int* arr, size_t size) {
    for (size_t i = 0; i < size; ++i) {
        std::cout << arr[i] << " ";
    }
    std::cout << std::endl;
}

// 方式 2：传递数组引用（需要知道大小）
void print_array2(int (&arr)[5]) {
    for (int elem : arr) {
        std::cout << elem << " ";
    }
    std::cout << std::endl;
}

// 方式 3：使用 std::array（推荐）
#include <array>
void print_array3(const std::array<int, 5>& arr) {
    for (int elem : arr) {
        std::cout << elem << " ";
    }
    std::cout << std::endl;
}

// 方式 4：使用 std::vector（推荐）
#include <vector>
void print_array4(const std::vector<int>& vec) {
    for (int elem : vec) {
        std::cout << elem << " ";
    }
    std::cout << std::endl;
}

int main() {
    int arr[5] = {1, 2, 3, 4, 5};
    print_array1(arr, 5);
    print_array2(arr);
    
    std::array<int, 5> arr2 = {1, 2, 3, 4, 5};
    print_array3(arr2);
    
    std::vector<int> vec = {1, 2, 3, 4, 5};
    print_array4(vec);
    
    return 0;
}
```

## 返回值

### 基本返回值

```cpp
#include <iostream>
#include <string>

// 返回基本类型
int add(int a, int b) {
    return a + b;
}

// 返回字符串
std::string get_greeting() {
    return "Hello, C++!";
}

// 返回布尔值
bool is_even(int n) {
    return n % 2 == 0;
}

// 无返回值
void print_message(const std::string& msg) {
    std::cout << msg << std::endl;
    // 可以显式返回，也可以省略
    return;
}

int main() {
    std::cout << add(3, 4) << std::endl;
    std::cout << get_greeting() << std::endl;
    std::cout << std::boolalpha << is_even(4) << std::endl;
    print_message("Test");
    
    return 0;
}
```

### 返回引用

```cpp
#include <iostream>
#include <vector>

// 返回引用（避免复制）
int& get_element(std::vector<int>& vec, size_t index) {
    return vec[index];
}

// 返回常量引用（只读）
const std::string& get_name(const std::string& name) {
    return name;  // 返回引用，不复制
}

int main() {
    std::vector<int> vec = {10, 20, 30, 40, 50};
    
    // 通过引用修改元素
    get_element(vec, 2) = 100;
    std::cout << vec[2] << std::endl;  // 100
    
    std::string name = "Alice";
    const std::string& ref = get_name(name);
    std::cout << ref << std::endl;
    
    return 0;
}
```

### 返回多个值

```cpp
#include <iostream>
#include <tuple>
#include <utility>

// 方式 1：使用 std::pair
std::pair<int, int> divide_with_remainder(int dividend, int divisor) {
    return {dividend / divisor, dividend % divisor};
}

// 方式 2：使用 std::tuple（C++11）
std::tuple<int, double, std::string> get_info() {
    return std::make_tuple(42, 3.14, "Hello");
}

// 方式 3：使用结构体（推荐，可读性好）
struct Result {
    int quotient;
    int remainder;
};

Result divide(int dividend, int divisor) {
    return {dividend / divisor, dividend % divisor};
}

int main() {
    // 使用 std::pair
    auto [q1, r1] = divide_with_remainder(17, 5);  // C++17 结构化绑定
    std::cout << "Quotient: " << q1 << ", Remainder: " << r1 << std::endl;
    
    // 使用 std::tuple
    auto [num, pi, msg] = get_info();
    std::cout << num << ", " << pi << ", " << msg << std::endl;
    
    // 使用结构体
    Result result = divide(17, 5);
    std::cout << "Quotient: " << result.quotient 
              << ", Remainder: " << result.remainder << std::endl;
    
    return 0;
}
```

## 函数重载

### 基本重载

```cpp
#include <iostream>
#include <string>

// 函数重载：同名函数，不同参数
int add(int a, int b) {
    return a + b;
}

double add(double a, double b) {
    return a + b;
}

int add(int a, int b, int c) {
    return a + b + c;
}

std::string add(const std::string& a, const std::string& b) {
    return a + b;
}

int main() {
    std::cout << add(3, 4) << std::endl;              // 调用 int add(int, int)
    std::cout << add(3.5, 4.2) << std::endl;         // 调用 double add(double, double)
    std::cout << add(1, 2, 3) << std::endl;          // 调用 int add(int, int, int)
    std::cout << add(std::string("Hello"), std::string("World")) << std::endl;
    
    return 0;
}
```

### 重载规则

```cpp
#include <iostream>

// 重载解析规则
void print(int x) {
    std::cout << "int: " << x << std::endl;
}

void print(double x) {
    std::cout << "double: " << x << std::endl;
}

void print(const char* x) {
    std::cout << "const char*: " << x << std::endl;
}

int main() {
    print(42);        // 调用 print(int)
    print(3.14);      // 调用 print(double)
    print("Hello");   // 调用 print(const char*)
    
    // 注意：不能仅通过返回类型重载
    // int get_value();
    // double get_value();  // 错误！
    
    return 0;
}
```

## 默认参数

### 基本用法

```cpp
#include <iostream>
#include <string>

// 默认参数：从右到左必须连续
void greet(const std::string& name, const std::string& prefix = "Hello") {
    std::cout << prefix << ", " << name << "!" << std::endl;
}

// 多个默认参数
void create_window(int width = 800, int height = 600, 
                   const std::string& title = "Window") {
    std::cout << "Window: " << width << "x" << height 
              << " - " << title << std::endl;
}

int main() {
    greet("Alice");                    // 使用默认 prefix
    greet("Bob", "Hi");                // 指定 prefix
    
    create_window();                   // 全部使用默认值
    create_window(1024);               // 只指定 width
    create_window(1024, 768);          // 指定 width 和 height
    create_window(1024, 768, "My App"); // 全部指定
    
    return 0;
}
```

### 默认参数与函数重载

```cpp
#include <iostream>

// 默认参数可以替代部分重载
void print(int x, int base = 10) {
    if (base == 10) {
        std::cout << "Decimal: " << x << std::endl;
    } else if (base == 16) {
        std::cout << "Hex: 0x" << std::hex << x << std::endl;
    }
}

int main() {
    print(42);      // 使用默认 base=10
    print(42, 16);  // 指定 base=16
    
    return 0;
}
```

## 内联函数

### inline 关键字

```cpp
#include <iostream>

// 内联函数：建议编译器在调用处展开代码
inline int square(int x) {
    return x * x;
}

// 类内定义的成员函数自动内联
class Math {
public:
    int multiply(int a, int b) {  // 自动内联
        return a * b;
    }
};

int main() {
    int result = square(5);  // 可能被展开为: int result = 5 * 5;
    std::cout << result << std::endl;
    
    return 0;
}
```

**注意**：`inline` 只是建议，编译器可能忽略。现代编译器会自动内联小函数。

## Lambda 表达式（C++11）

### 基本语法

```cpp
#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    // Lambda 表达式语法：
    // [捕获列表](参数列表) -> 返回类型 { 函数体 }
    
    // 基本 lambda
    auto add = [](int a, int b) -> int {
        return a + b;
    };
    std::cout << add(3, 4) << std::endl;
    
    // 简化语法（返回类型自动推断）
    auto multiply = [](int a, int b) {
        return a * b;
    };
    std::cout << multiply(3, 4) << std::endl;
    
    // 无参数 lambda
    auto greet = []() {
        std::cout << "Hello, Lambda!" << std::endl;
    };
    greet();
    
    // 在算法中使用
    std::vector<int> vec = {1, 2, 3, 4, 5};
    std::for_each(vec.begin(), vec.end(), [](int x) {
        std::cout << x * 2 << " ";
    });
    std::cout << std::endl;
    
    return 0;
}
```

### 捕获列表

```cpp
#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    int multiplier = 10;
    std::vector<int> vec = {1, 2, 3, 4, 5};
    
    // [=] 值捕获（复制）
    auto multiply_by = [=](int x) {
        return x * multiplier;  // 使用外部变量 multiplier
    };
    std::cout << multiply_by(5) << std::endl;  // 50
    
    // [&] 引用捕获
    int sum = 0;
    std::for_each(vec.begin(), vec.end(), [&sum](int x) {
        sum += x;  // 修改外部变量 sum
    });
    std::cout << "Sum: " << sum << std::endl;
    
    // 混合捕获
    int a = 1, b = 2, c = 3;
    auto mixed = [=, &sum](int x) {
        // a, b, c 值捕获，sum 引用捕获
        sum += x + a + b + c;
    };
    
    // 指定捕获变量
    auto specific = [multiplier, &sum](int x) {
        sum += x * multiplier;
    };
    
    return 0;
}
```

### 泛型 Lambda（C++14）

```cpp
#include <iostream>

int main() {
    // 使用 auto 作为参数类型（C++14）
    auto add = [](auto a, auto b) {
        return a + b;
    };
    
    std::cout << add(3, 4) << std::endl;        // 7
    std::cout << add(3.5, 4.2) << std::endl;    // 7.7
    std::cout << add(std::string("Hello"), std::string("World")) << std::endl;
    
    return 0;
}
```

## 函数指针

### 基本用法

```cpp
#include <iostream>

int add(int a, int b) {
    return a + b;
}

int multiply(int a, int b) {
    return a * b;
}

int main() {
    // 函数指针声明
    int (*func_ptr)(int, int);
    
    // 赋值
    func_ptr = add;
    std::cout << func_ptr(3, 4) << std::endl;  // 7
    
    func_ptr = multiply;
    std::cout << func_ptr(3, 4) << std::endl;  // 12
    
    // 使用 typedef 简化
    using FuncType = int (*)(int, int);
    FuncType f = add;
    std::cout << f(5, 6) << std::endl;
    
    return 0;
}
```

### 函数指针作为参数

```cpp
#include <iostream>
#include <vector>

int add(int a, int b) { return a + b; }
int multiply(int a, int b) { return a * b; }

// 函数指针作为参数
void calculate(int a, int b, int (*op)(int, int)) {
    std::cout << "Result: " << op(a, b) << std::endl;
}

int main() {
    calculate(10, 5, add);        // Result: 15
    calculate(10, 5, multiply);   // Result: 50
    
    return 0;
}
```

### std::function（C++11，推荐）

```cpp
#include <iostream>
#include <functional>

int add(int a, int b) { return a + b; }

int main() {
    // std::function 可以存储任何可调用对象
    std::function<int(int, int)> func = add;
    std::cout << func(3, 4) << std::endl;
    
    // 存储 lambda
    func = [](int a, int b) { return a * b; };
    std::cout << func(3, 4) << std::endl;
    
    return 0;
}
```

## 递归函数

```cpp
#include <iostream>

// 递归：函数调用自身
int factorial(int n) {
    if (n <= 1) {
        return 1;  // 基准情况
    }
    return n * factorial(n - 1);  // 递归调用
}

int fibonacci(int n) {
    if (n <= 1) {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

int main() {
    std::cout << "5! = " << factorial(5) << std::endl;        // 120
    std::cout << "fib(10) = " << fibonacci(10) << std::endl; // 55
    
    return 0;
}
```

## 实践练习

### 练习 1：计算器

```cpp
#include <iostream>
#include <functional>
#include <map>

int add(int a, int b) { return a + b; }
int subtract(int a, int b) { return a - b; }
int multiply(int a, int b) { return a * b; }
int divide(int a, int b) { return (b != 0) ? a / b : 0; }

int main() {
    std::map<char, std::function<int(int, int)>> operations = {
        {'+', add},
        {'-', subtract},
        {'*', multiply},
        {'/', divide}
    };
    
    int a, b;
    char op;
    std::cout << "Enter expression (e.g., 10 + 5): ";
    std::cin >> a >> op >> b;
    
    if (operations.find(op) != operations.end()) {
        std::cout << "Result: " << operations[op](a, b) << std::endl;
    } else {
        std::cout << "Invalid operator!" << std::endl;
    }
    
    return 0;
}
```

### 练习 2：使用 Lambda 排序

```cpp
#include <iostream>
#include <vector>
#include <algorithm>

struct Person {
    std::string name;
    int age;
};

int main() {
    std::vector<Person> people = {
        {"Alice", 25},
        {"Bob", 30},
        {"Charlie", 20}
    };
    
    // 按年龄排序
    std::sort(people.begin(), people.end(), 
              [](const Person& a, const Person& b) {
                  return a.age < b.age;
              });
    
    for (const auto& p : people) {
        std::cout << p.name << " (" << p.age << ")" << std::endl;
    }
    
    return 0;
}
```

## 与其他语言对比

| 特性 | C++ | Java | Python | Go | Rust |
|------|-----|------|--------|-----|------|
| **函数定义** | `int f(int x)` | `int f(int x)` | `def f(x):` | `func f(x int) int` | `fn f(x: i32) -> i32` |
| **默认参数** | ✅ | ❌ | ✅ | ❌ | ❌ |
| **函数重载** | ✅ | ✅ | ❌ | ❌ | ❌ |
| **Lambda** | ✅ (C++11) | ✅ (Java 8) | ✅ | ❌ | ✅ |
| **函数指针** | ✅ | ❌ | ❌ | ✅ | ✅ |
| **内联函数** | ✅ | ❌ | ❌ | ❌ | ✅ |

## 下一步

现在你已经掌握了函数，可以继续学习：

1. **[面向对象编程](./06-面向对象编程.md)** - 学习类和对象
2. **[模板编程](./08-模板编程.md)** - 掌握泛型编程
3. **[STL 标准库](./09-STL标准库.md)** - 学习标准库的使用

祝你学习愉快！🎉

