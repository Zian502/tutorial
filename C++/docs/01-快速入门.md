# 快速入门

## 环境搭建

### 安装 C++ 编译器

C++ 是编译型语言，需要编译器将源代码编译成可执行文件。

#### macOS

```bash
# 使用 Homebrew 安装 GCC
brew install gcc

# 或安装 Clang（macOS 自带，但版本可能较旧）
# 安装最新版 Clang
brew install llvm

# 验证安装
g++ --version
# 输出：g++ (GCC) 13.2.0

clang++ --version
# 输出：clang version 17.0.0
```

#### Linux (Ubuntu/Debian)

```bash
# 安装 GCC
sudo apt update
sudo apt install build-essential

# 验证安装
g++ --version
# 输出：g++ (GCC) 13.2.0
```

#### Linux (CentOS/RHEL)

```bash
# 安装 GCC
sudo yum groupinstall "Development Tools"
# 或
sudo dnf groupinstall "Development Tools"

# 验证安装
g++ --version
```

#### Windows

**方法 1：使用 MinGW-w64**

1. 下载 MinGW-w64：https://www.mingw-w64.org/
2. 或使用 MSYS2：https://www.msys2.org/
3. 安装后添加到 PATH 环境变量

**方法 2：使用 Visual Studio**

1. 下载 Visual Studio Community（免费）
2. 安装时选择 "使用 C++ 的桌面开发" 工作负载
3. 包含 MSVC 编译器和调试器

**方法 3：使用 WSL（Windows Subsystem for Linux）**

```bash
# 在 WSL 中安装 GCC
sudo apt update
sudo apt install build-essential
```

### 配置开发环境

#### VS Code（推荐）

1. 安装 VS Code
2. 安装扩展：
   - **C/C++**（Microsoft）
   - **C/C++ Extension Pack**（包含多个相关扩展）
   - **CMake Tools**（如果使用 CMake）

3. 配置 `tasks.json`（用于编译）：
```json
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++",
            "args": [
                "-std=c++20",
                "-Wall",
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
```

#### CLion（JetBrains IDE）

- 专业 C++ IDE，功能完善
- 内置 CMake 支持
- 强大的调试和代码分析功能

#### Visual Studio（Windows）

- 微软官方 IDE
- 强大的调试器
- 完整的 Windows 开发工具链

#### Qt Creator

- 跨平台 IDE
- 适合 Qt 应用开发
- 内置 CMake 和 qmake 支持

### 验证安装

```bash
# 检查编译器版本
g++ --version
# 或
clang++ --version

# 检查 C++ 标准支持
g++ -std=c++20 --version
```

## 第一个 C++ 程序

### Hello World

创建 `hello.cpp` 文件：

```cpp
#include <iostream>

int main() {
    std::cout << "Hello, C++!" << std::endl;
    return 0;
}
```

### 编译和运行

```bash
# 编译
g++ -std=c++20 -o hello hello.cpp

# 运行
./hello
# 输出：Hello, C++!

# Windows 上运行
hello.exe
```

### 代码解释

```cpp
// 1. 头文件包含
#include <iostream>
// iostream 是输入输出流库，提供 cin、cout 等功能
// 类似其他语言：
// - Python: print()
// - Java: System.out.println()
// - Go: fmt.Println()

// 2. main 函数
int main() {
    // main 是程序的入口点
    // 返回类型是 int（0 表示成功）
    
    // 3. 输出语句
    std::cout << "Hello, C++!" << std::endl;
    // std::cout: 标准输出流
    // <<: 流插入运算符
    // std::endl: 换行并刷新缓冲区
    
    // 4. 返回值
    return 0;  // 0 表示程序正常退出
}
```

### 与其他语言对比

| 语言 | Hello World 示例 |
|------|-----------------|
| **C++** | `std::cout << "Hello" << std::endl;` |
| **C** | `printf("Hello\n");` |
| **Python** | `print("Hello")` |
| **Java** | `System.out.println("Hello");` |
| **Go** | `fmt.Println("Hello")` |
| **Rust** | `println!("Hello");` |
| **JavaScript** | `console.log("Hello");` |

## 编译选项

### 常用编译选项

```bash
# 基本编译
g++ source.cpp -o output

# 指定 C++ 标准
g++ -std=c++20 source.cpp -o output
# 可选值：c++98, c++03, c++11, c++14, c++17, c++20, c++23

# 启用警告
g++ -Wall -Wextra source.cpp -o output
# -Wall: 启用所有常见警告
# -Wextra: 启用额外警告

# 调试信息
g++ -g source.cpp -o output
# -g: 生成调试信息，用于 GDB 调试

# 优化级别
g++ -O2 source.cpp -o output
# -O0: 无优化（默认，适合调试）
# -O1: 基本优化
# -O2: 推荐优化级别
# -O3: 最大优化（可能增加编译时间）

# 多文件编译
g++ -std=c++20 main.cpp utils.cpp -o program

# 生成目标文件（不链接）
g++ -c source.cpp -o source.o

# 链接目标文件
g++ main.o utils.o -o program
```

### CMake 构建系统（推荐）

创建 `CMakeLists.txt`：

```cmake
cmake_minimum_required(VERSION 3.20)
project(HelloCpp)

# 设置 C++ 标准
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# 添加可执行文件
add_executable(hello hello.cpp)

# 添加编译选项
target_compile_options(hello PRIVATE -Wall -Wextra)

# 添加包含目录
# target_include_directories(hello PRIVATE include)

# 添加库
# target_link_libraries(hello PRIVATE some_library)
```

使用 CMake：

```bash
# 创建构建目录
mkdir build
cd build

# 生成构建文件
cmake ..

# 编译
cmake --build .

# 运行
./hello
```

## 项目结构

### 简单项目结构

```
my-project/
├── main.cpp          # 主程序入口
├── utils.h           # 头文件
├── utils.cpp         # 实现文件
└── CMakeLists.txt    # CMake 配置文件
```

### 标准项目结构

```
my-project/
├── src/              # 源代码目录
│   ├── main.cpp
│   ├── utils.cpp
│   └── math.cpp
├── include/          # 头文件目录
│   ├── utils.h
│   └── math.h
├── tests/            # 测试目录
│   └── test_utils.cpp
├── CMakeLists.txt    # CMake 配置
└── README.md         # 项目说明
```

## 输入输出

### 标准输出

```cpp
#include <iostream>

int main() {
    // 方式 1：使用 std::cout
    std::cout << "Hello" << std::endl;
    
    // 方式 2：使用 \n（更高效，不刷新缓冲区）
    std::cout << "World\n";
    
    // 方式 3：输出变量
    int age = 25;
    std::cout << "Age: " << age << std::endl;
    
    // 方式 4：格式化输出（C++20）
    std::cout << std::format("Age: {}\n", age);
    
    return 0;
}
```

### 标准输入

```cpp
#include <iostream>
#include <string>

int main() {
    // 读取整数
    int number;
    std::cout << "Enter a number: ";
    std::cin >> number;
    std::cout << "You entered: " << number << std::endl;
    
    // 读取字符串（遇到空格停止）
    std::string name;
    std::cout << "Enter your name: ";
    std::cin >> name;
    std::cout << "Hello, " << name << std::endl;
    
    // 读取整行（包含空格）
    std::string fullName;
    std::cout << "Enter your full name: ";
    std::cin.ignore();  // 忽略之前的换行符
    std::getline(std::cin, fullName);
    std::cout << "Full name: " << fullName << std::endl;
    
    return 0;
}
```

### 与其他语言对比

| 操作 | C++ | Python | Java | Go |
|------|-----|--------|------|-----|
| **输出** | `std::cout << x` | `print(x)` | `System.out.println(x)` | `fmt.Println(x)` |
| **输入** | `std::cin >> x` | `input()` | `Scanner.next()` | `fmt.Scan()` |
| **格式化** | `std::format()` (C++20) | `f"{}"` | `String.format()` | `fmt.Printf()` |

## 命名空间

### 使用命名空间

```cpp
#include <iostream>

// 方式 1：使用完整命名空间
int main() {
    std::cout << "Hello" << std::endl;
    return 0;
}

// 方式 2：使用 using 声明
using std::cout;
using std::endl;

int main() {
    cout << "Hello" << endl;
    return 0;
}

// 方式 3：使用 using namespace（不推荐在头文件中使用）
using namespace std;

int main() {
    cout << "Hello" << endl;
    return 0;
}
```

### 自定义命名空间

```cpp
#include <iostream>

namespace mylib {
    void print(const std::string& msg) {
        std::cout << msg << std::endl;
    }
    
    namespace math {
        int add(int a, int b) {
            return a + b;
        }
    }
}

int main() {
    // 使用命名空间
    mylib::print("Hello");
    
    // 嵌套命名空间
    int result = mylib::math::add(3, 4);
    std::cout << "Result: " << result << std::endl;
    
    // 使用 using
    using namespace mylib;
    print("World");
    
    return 0;
}
```

## 注释

```cpp
// 单行注释

/*
 * 多行注释
 * 可以写多行内容
 */

// C++ 风格注释（推荐）
// 简洁明了

/* C 风格注释 */
// 适合大段注释
```

## 预处理器指令

```cpp
// 包含头文件
#include <iostream>      // 系统头文件（尖括号）
#include "myheader.h"    // 本地头文件（引号）

// 宏定义
#define PI 3.14159
#define MAX(a, b) ((a) > (b) ? (a) : (b))

// 条件编译
#ifdef DEBUG
    std::cout << "Debug mode" << std::endl;
#endif

#ifndef MY_HEADER_H
#define MY_HEADER_H
// 头文件内容
#endif

// 编译时检查
#if __cplusplus >= 202002L
    // C++20 特性
#endif
```

## 实践练习

### 练习 1：计算器

```cpp
#include <iostream>

int main() {
    double a, b;
    char op;
    
    std::cout << "Enter two numbers and an operator (+, -, *, /): ";
    std::cin >> a >> op >> b;
    
    double result = 0;
    switch (op) {
        case '+':
            result = a + b;
            break;
        case '-':
            result = a - b;
            break;
        case '*':
            result = a * b;
            break;
        case '/':
            if (b != 0) {
                result = a / b;
            } else {
                std::cout << "Error: Division by zero!" << std::endl;
                return 1;
            }
            break;
        default:
            std::cout << "Error: Invalid operator!" << std::endl;
            return 1;
    }
    
    std::cout << a << " " << op << " " << b << " = " << result << std::endl;
    return 0;
}
```

### 练习 2：温度转换

```cpp
#include <iostream>

int main() {
    double celsius;
    std::cout << "Enter temperature in Celsius: ";
    std::cin >> celsius;
    
    double fahrenheit = (celsius * 9.0 / 5.0) + 32.0;
    std::cout << celsius << "°C = " << fahrenheit << "°F" << std::endl;
    
    return 0;
}
```

## 常见问题

### 1. 编译错误：找不到头文件

```bash
# 解决方案：检查头文件路径
g++ -I/path/to/include source.cpp -o output
```

### 2. 链接错误：找不到库

```bash
# 解决方案：指定库路径和库名
g++ source.cpp -L/path/to/lib -lmylib -o output
```

### 3. 运行时错误：段错误（Segmentation Fault）

- 检查指针是否为空
- 检查数组越界
- 使用调试器定位问题：`gdb ./program`

### 4. 未定义行为（Undefined Behavior）

- 使用未初始化的变量
- 数组越界访问
- 空指针解引用
- 使用已释放的内存

## 下一步

现在你已经完成了快速入门，可以继续学习：

1. **[基础语法](./02-基础语法.md)** - 学习变量、常量、基本语法规则
2. **[数据类型](./03-数据类型.md)** - 了解 C++ 的数据类型系统
3. **[控制结构](./04-控制结构.md)** - 掌握条件语句和循环

祝你学习愉快！🎉

