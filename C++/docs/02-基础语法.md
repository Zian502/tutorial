# 基础语法

## 变量和常量

### 变量声明

C++ 是静态类型语言，变量使用前必须声明类型。

```cpp
#include <iostream>

int main() {
    // 基本变量声明
    int age = 25;
    double price = 99.99;
    char grade = 'A';
    bool isActive = true;
    
    // 声明后赋值
    int count;
    count = 10;
    
    // 多个变量声明
    int x = 1, y = 2, z = 3;
    
    // 类型推断（C++11）
    auto name = "C++";  // 推断为 const char*
    auto number = 42;   // 推断为 int
    auto pi = 3.14;     // 推断为 double
    
    return 0;
}
```

### 变量命名规则

```cpp
// 合法的变量名
int age = 25;
int user_age = 30;      // 下划线命名
int userName = 40;      // 驼峰命名
int _private = 50;     // 下划线开头（约定为私有）
int MAX_SIZE = 100;     // 全大写表示常量（约定）

// 不合法的变量名
// int 2name = 10;      // 不能以数字开头
// int user-name = 20;  // 不能包含连字符
// int class = 30;      // 不能使用关键字
```

### 常量

```cpp
#include <iostream>

int main() {
    // 方式 1：const 关键字（推荐）
    const int MAX_SIZE = 100;
    const double PI = 3.14159;
    const std::string APP_NAME = "MyApp";
    
    // const 变量必须在声明时初始化
    // const int x;  // 错误！
    
    // 方式 2：constexpr（C++11，编译时常量）
    constexpr int BUFFER_SIZE = 1024;
    constexpr double E = 2.71828;
    
    // constexpr 必须是编译时可计算的
    constexpr int square(int x) {
        return x * x;
    }
    constexpr int result = square(5);  // 编译时计算
    
    // 方式 3：宏定义（不推荐，C 风格）
    #define MAX_VALUE 1000
    #define MIN_VALUE 0
    
    // 方式 4：枚举常量
    enum { RED, GREEN, BLUE };
    
    return 0;
}
```

### const vs constexpr

| 特性 | const | constexpr |
|------|-------|-----------|
| **初始化时机** | 运行时或编译时 | 必须是编译时 |
| **类型** | 可以是任何类型 | 必须是字面量类型 |
| **函数** | 不能用于函数 | 可以用于函数（编译时求值） |
| **灵活性** | 更灵活 | 更严格，性能更好 |

```cpp
#include <iostream>

int main() {
    int runtime_value;
    std::cin >> runtime_value;
    
    // const 可以接受运行时值
    const int const_val = runtime_value;  // OK
    
    // constexpr 必须是编译时值
    // constexpr int constexpr_val = runtime_value;  // 错误！
    
    constexpr int compile_time = 100;  // OK
    
    return 0;
}
```

### 与其他语言对比

| 语言 | 变量声明 | 常量声明 | 类型推断 |
|------|---------|---------|---------|
| **C++** | `int x = 5;` | `const int x = 5;` | `auto x = 5;` (C++11) |
| **C** | `int x = 5;` | `const int x = 5;` | ❌ |
| **Java** | `int x = 5;` | `final int x = 5;` | `var x = 5;` (Java 10+) |
| **Python** | `x = 5` | 约定：`X = 5` | ✅ 默认 |
| **Go** | `var x int = 5` | `const x = 5` | `x := 5` |
| **Rust** | `let x = 5;` | `const X: i32 = 5;` | ✅ 默认 |

## 基本数据类型

### 整数类型

```cpp
#include <iostream>
#include <cstdint>  // 固定宽度整数类型

int main() {
    // 有符号整数
    signed char c = 127;        // 至少 8 位，通常 -128 到 127
    short s = 32767;            // 至少 16 位
    int i = 2147483647;         // 至少 16 位，通常 32 位
    long l = 2147483647L;       // 至少 32 位
    long long ll = 9223372036854775807LL;  // 至少 64 位
    
    // 无符号整数
    unsigned char uc = 255;
    unsigned short us = 65535;
    unsigned int ui = 4294967295U;
    unsigned long ul = 4294967295UL;
    unsigned long long ull = 18446744073709551615ULL;
    
    // 固定宽度类型（C++11，推荐）
    int8_t i8 = 127;            // 精确 8 位
    int16_t i16 = 32767;        // 精确 16 位
    int32_t i32 = 2147483647;   // 精确 32 位
    int64_t i64 = 9223372036854775807LL;  // 精确 64 位
    
    uint8_t u8 = 255;
    uint16_t u16 = 65535;
    uint32_t u32 = 4294967295U;
    uint64_t u64 = 18446744073709551615ULL;
    
    // 字面量
    int decimal = 42;
    int octal = 052;           // 八进制：42
    int hex = 0x2A;            // 十六进制：42
    int binary = 0b101010;     // 二进制：42 (C++14)
    
    // 数字分隔符（C++14）
    long long big = 1'000'000'000;
    int hex_sep = 0xFF'FF'FF'FF;
    
    return 0;
}
```

### 浮点类型

```cpp
#include <iostream>
#include <iomanip>

int main() {
    // float：单精度（通常 32 位，约 7 位有效数字）
    float f = 3.14f;
    float f2 = 3.14F;  // 大小写都可以
    
    // double：双精度（通常 64 位，约 15 位有效数字，默认）
    double d = 3.141592653589793;
    double d2 = 3.14;  // 默认是 double
    
    // long double：扩展精度（精度取决于实现）
    long double ld = 3.141592653589793238L;
    
    // 科学计数法
    double sci = 1.5e10;      // 1.5 × 10^10
    double sci2 = 1.5E-5;     // 1.5 × 10^-5
    
    // 精度示例
    std::cout << std::setprecision(15);
    std::cout << "float:  " << f << std::endl;
    std::cout << "double: " << d << std::endl;
    
    return 0;
}
```

### 字符类型

```cpp
#include <iostream>
#include <string>

int main() {
    // char：字符类型（通常 8 位）
    char c = 'A';
    char c2 = 65;  // ASCII 值
    
    // signed char / unsigned char
    signed char sc = -128;
    unsigned char uc = 255;
    
    // wchar_t：宽字符（用于 Unicode）
    wchar_t wc = L'中';
    
    // char16_t / char32_t（C++11，用于 UTF-16/UTF-32）
    char16_t c16 = u'中';
    char32_t c32 = U'中';
    
    // 字符字面量
    char newline = '\n';
    char tab = '\t';
    char quote = '\'';
    char backslash = '\\';
    char null = '\0';
    
    // 转义序列
    std::cout << "Hello\nWorld\tTab" << std::endl;
    
    return 0;
}
```

### 布尔类型

```cpp
#include <iostream>

int main() {
    // bool：布尔类型
    bool isTrue = true;
    bool isFalse = false;
    
    // 布尔值可以转换为整数
    int true_as_int = true;   // 1
    int false_as_int = false; // 0
    
    // 非零值转换为 true，零值转换为 false
    bool from_int = 42;       // true
    bool from_zero = 0;       // false
    
    // 布尔运算
    bool result1 = true && false;  // false
    bool result2 = true || false;   // true
    bool result3 = !true;            // false
    
    std::cout << std::boolalpha;  // 输出 true/false 而不是 1/0
    std::cout << "isTrue: " << isTrue << std::endl;
    
    return 0;
}
```

### void 类型

```cpp
// void 表示"无类型"
void function();        // 无返回值的函数
void* ptr;              // 指向任意类型的指针
```

### 类型大小和范围

```cpp
#include <iostream>
#include <limits>
#include <cstdint>

int main() {
    // 查看类型大小（字节）
    std::cout << "Size of int: " << sizeof(int) << " bytes" << std::endl;
    std::cout << "Size of double: " << sizeof(double) << " bytes" << std::endl;
    
    // 查看类型范围
    std::cout << "int range: [" 
              << std::numeric_limits<int>::min() << ", "
              << std::numeric_limits<int>::max() << "]" << std::endl;
    
    std::cout << "double range: [" 
              << std::numeric_limits<double>::lowest() << ", "
              << std::numeric_limits<double>::max() << "]" << std::endl;
    
    return 0;
}
```

## 类型转换

### 隐式类型转换

```cpp
#include <iostream>

int main() {
    // 整数提升
    char c = 'A';
    int i = c;  // char 提升为 int
    
    // 浮点转换
    int x = 3;
    double y = x;  // int 转换为 double
    
    // 算术转换
    int a = 5;
    double b = 3.14;
    double result = a + b;  // a 转换为 double
    
    // 布尔转换
    bool flag = 42;  // 非零值转换为 true
    
    return 0;
}
```

### 显式类型转换

```cpp
#include <iostream>

int main() {
    double pi = 3.14159;
    
    // 方式 1：C 风格转换（不推荐）
    int pi_int1 = (int)pi;
    
    // 方式 2：函数风格转换（不推荐）
    int pi_int2 = int(pi);
    
    // 方式 3：static_cast（推荐，编译时检查）
    int pi_int3 = static_cast<int>(pi);
    
    // 方式 4：const_cast（移除 const）
    const int* ptr1 = nullptr;
    int* ptr2 = const_cast<int*>(ptr1);
    
    // 方式 5：reinterpret_cast（危险，重新解释位模式）
    int* ptr3 = reinterpret_cast<int*>(0x1234);
    
    // 方式 6：dynamic_cast（运行时类型检查，用于多态）
    // Base* base = new Derived();
    // Derived* derived = dynamic_cast<Derived*>(base);
    
    return 0;
}
```

### 类型别名

```cpp
#include <iostream>

// 方式 1：typedef（C 风格）
typedef int Age;
typedef double Price;

// 方式 2：using（C++11，推荐）
using Age = int;
using Price = double;
using StringVector = std::vector<std::string>;

// 模板别名（C++11）
template<typename T>
using Vector = std::vector<T>;

int main() {
    Age age = 25;
    Price price = 99.99;
    
    Vector<int> numbers;  // 等价于 std::vector<int>
    
    return 0;
}
```

## 作用域和生命周期

### 作用域

```cpp
#include <iostream>

int global_var = 100;  // 全局作用域

void function() {
    int local_var = 50;  // 局部作用域
    
    {
        int block_var = 25;  // 块作用域
        std::cout << block_var << std::endl;
    }
    // block_var 在这里不可访问
    
    std::cout << local_var << std::endl;
    std::cout << global_var << std::endl;
}

int main() {
    int main_var = 10;
    function();
    // local_var 在这里不可访问
    std::cout << global_var << std::endl;
    return 0;
}
```

### 命名空间作用域

```cpp
#include <iostream>

namespace outer {
    int x = 1;
    
    namespace inner {
        int x = 2;
    }
}

int main() {
    std::cout << outer::x << std::endl;           // 1
    std::cout << outer::inner::x << std::endl;    // 2
    
    using namespace outer;
    std::cout << x << std::endl;                  // 1
    std::cout << inner::x << std::endl;           // 2
    
    return 0;
}
```

### 存储期

```cpp
#include <iostream>

int global = 100;           // 静态存储期
static int file_static = 200;  // 文件作用域静态

void function() {
    static int func_static = 300;  // 函数静态（只初始化一次）
    int local = 400;                // 自动存储期
    
    func_static++;
    std::cout << "func_static: " << func_static << std::endl;
    std::cout << "local: " << local << std::endl;
}

int main() {
    function();  // func_static: 301, local: 400
    function();  // func_static: 302, local: 400
    
    return 0;
}
```

## 关键字和标识符

### C++ 关键字（C++20）

```cpp
// C++ 关键字列表（部分）
// alignas, alignof, and, and_eq, asm, auto, bitand, bitor, bool,
// break, case, catch, char, char8_t, char16_t, char32_t, class,
// compl, concept, const, consteval, constexpr, const_cast, continue,
// co_await, co_return, co_yield, decltype, default, delete, do,
// double, dynamic_cast, else, enum, explicit, export, extern, false,
// float, for, friend, goto, if, inline, int, long, mutable, namespace,
// new, noexcept, not, not_eq, nullptr, operator, or, or_eq, private,
// protected, public, register, reinterpret_cast, requires, return,
// short, signed, sizeof, static, static_assert, static_cast, struct,
// switch, template, this, thread_local, throw, true, try, typedef,
// typeid, typename, union, unsigned, using, virtual, void, volatile,
// wchar_t, while, xor, xor_eq
```

### 标识符规则

```cpp
// 合法标识符
int variable_name;
int variableName;
int _private;
int MAX_SIZE;
int name123;

// 不合法标识符
// int 123name;      // 不能以数字开头
// int name-name;    // 不能包含连字符
// int class;        // 不能使用关键字
```

## 注释

```cpp
// 单行注释

/*
 * 多行注释
 * 可以写多行内容
 */

// C++ 风格注释（推荐）
// 简洁明了

/* C 风格注释 */
// 适合大段注释

// 文档注释（Doxygen 风格）
/**
 * @brief 函数说明
 * @param x 参数说明
 * @return 返回值说明
 */
int function(int x);
```

## 实践练习

### 练习 1：类型转换

```cpp
#include <iostream>

int main() {
    double pi = 3.14159;
    int pi_int = static_cast<int>(pi);
    
    std::cout << "Original: " << pi << std::endl;
    std::cout << "Converted: " << pi_int << std::endl;
    
    return 0;
}
```

### 练习 2：作用域演示

```cpp
#include <iostream>

int global = 100;

void test_scope() {
    int local = 50;
    static int static_local = 0;
    
    static_local++;
    std::cout << "Global: " << global << std::endl;
    std::cout << "Local: " << local << std::endl;
    std::cout << "Static Local: " << static_local << std::endl;
}

int main() {
    test_scope();
    test_scope();
    test_scope();
    return 0;
}
```

## 与其他语言对比总结

| 特性 | C++ | Java | Python | Go | Rust |
|------|-----|------|--------|-----|------|
| **类型系统** | 静态强类型 | 静态强类型 | 动态类型 | 静态强类型 | 静态强类型 |
| **类型推断** | `auto` (C++11) | `var` (Java 10+) | 默认 | `:=` | 默认 |
| **常量** | `const` / `constexpr` | `final` | 约定 | `const` | `const` |
| **类型转换** | 多种 cast | 显式转换 | 自动 | 显式转换 | 显式转换 |
| **作用域** | 块作用域 | 块作用域 | 函数作用域 | 块作用域 | 块作用域 |

## 下一步

现在你已经掌握了基础语法，可以继续学习：

1. **[数据类型](./03-数据类型.md)** - 深入了解复合类型和自定义类型
2. **[控制结构](./04-控制结构.md)** - 学习条件语句和循环
3. **[函数](./05-函数.md)** - 掌握函数定义和使用

祝你学习愉快！🎉

